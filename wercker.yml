box: wercker-labs/docker
build:
  steps:
    - script:
        name: docker version
        code: |
          docker -v

    - script:
        name: pull docker image
        code: |
          docker pull litaio/ruby

